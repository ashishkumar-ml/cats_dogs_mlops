data:
  raw_dir: data/raw
  processed_dir: data/processed
  img_size: 224
  train_split: 0.8
  val_split: 0.1
  test_split: 0.1

train:
  epochs: 15
  batch_size: 32
  learning_rate: 0.001
  weight_decay: 0.0001
  model_path: models/best_model.pt
  experiment_name: cats-dogs-classification
  run_name: simple-cnn-baseline
